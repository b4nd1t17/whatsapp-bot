{
  "name": "dogma-moderador",
  "version": "3.2.0",
  "private": true,
  "type": "module",
  "description": "Dogma Moderador con SQLite, panel web, protecciones y sistema de permisos por roles.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check src/bot.js && node --check src/core/utils.js && node --check src/core/permissions.js && node --check src/services/contentFilters.js && node --check database/storage.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@whiskeysockets/baileys": "^7.0.0-rc.14",
    "express": "^5.1.0",
    "pino": "^9.9.0",
    "qrcode-terminal": "^0.12.0",
    "dotenv": "^17.2.1"
  }
}
